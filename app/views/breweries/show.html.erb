<p id="notice"><%= notice %></p>

<h2>
  <strong> 
   <%= @brewery.name %>
  </strong>
</h2>

<p>
  <em>Established at 
  <%= @brewery.year %></em>
</p>

<p>
  Number of beers 
  <%= @brewery.beers.count %>
</p>

<p>
 <% @brewery.beers.each do |beer| %>
   <%= link_to beer.name, beer %>
 <% end %>
</p>

<p>
<% if @brewery.ratings.empty? %>
 Brewery has no ratings!
<% else %>
 Has <%= pluralize(@brewery.ratings.count, 'rating', 'ratings') %>, average score: <%= @brewery.average_rating %>
<% end %>
</p>

<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
<%= link_to 'Back', breweries_path %>
